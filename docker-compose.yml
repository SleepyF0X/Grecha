version: '3.4'

services:
    ms-sql-server:
        image: mcr.microsoft.com/mssql/server:2017-latest-ubuntu
        environment:
            ACCEPT_EULA: "Y"
            SA_PASSWORD: "The_Best_Pa55"
            MSSQL_PID: Express
        ports:
            - "1433:1433"
    grecha:
        image: ${DOCKER_REGISTRY-}grecha
        build:
            context: .
            dockerfile: Grecha/Dockerfile
        ports:
            - "8080:80"
